# GaussMatrix for FreeBSD

GaussMatrix at the moment does not provide FreeBSD builds or have FreeBSD packaging, however GaussMatrix does
build and work on FreeBSD using the system-provided RocksDB.

Contributions for getting GaussMatrix into ports are welcome.
